RAM LOKESH — WOW ENTERPRISE 3D PORTFOLIO

Single-file static portfolio.
All HTML, CSS and JavaScript are inside index.html.

Run:
1. Extract the ZIP.
2. Open index.html in Chrome or Edge.

No npm, React, Node.js, separate CSS or JS files are required.

Before publishing, replace the generic LinkedIn and GitHub URLs in index.html with your actual profiles.
The resume button uses the resume short link supplied earlier.
